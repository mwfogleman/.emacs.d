# AGENTS.md

This file provides context for AI assistants working with this repository.

## Repository Overview

- **Type**: Emacs configuration using org-mode literate programming
- **Main file**: `tasshin.org` contains the literate configuration, loaded via `init.el`
- **Version control**: Git/GitHub
- **Repository**: `git@github.com:mwfogleman/.emacs.d.git`
- **Platform**: macOS
- **Shell**: Fish (note: config forces `/bin/bash` for Emacs shell commands to avoid compatibility issues)

## Key Features

- Org-mode literate configuration style
- Custom Obsidian vault integration functions
- Extensive key bindings and workflow customizations

## Working with This Config

- The actual configuration lives in `tasshin.org`, not scattered `.el` files
- Changes should be made to the org file, which tangles to elisp
- `custom.el` is auto-generated by Emacs - avoid manual edits
